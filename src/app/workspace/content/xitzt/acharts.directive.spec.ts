import { AchartsDirective } from './acharts.directive';

describe('AchartsDirective', () => {
  it('should create an instance', () => {
    const directive = new AchartsDirective();
    expect(directive).toBeTruthy();
  });
});
